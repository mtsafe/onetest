// functional test suite to be implemented
//    Test for dwt2pug() requires input file and output file
const { dwt2pug } = require('./dwt2pug.service')

describe('dwt2pug(): checking the dwt2pug module', () => {
  test('join vertical bar on its own line', () => {
    const input = 1
    const expected = 2
    const actual = 2
    expect(actual).toBe(expected)
    // Future implementation of test
  })
})
